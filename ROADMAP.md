# Editly — خطة البناء

> الحالة اليوم: المصادقة والملكية والرفع **حقيقية ومنشورة**. الذكاء والمونتاج والتصدير ما زالت محاكاة. هذه الوثيقة تحوّلها إلى منتج.

آخر تحديث: 7 أغسطس 2026 — بعد إنجاز المرحلتين ١ و٢

---

## أين نحن فعليًا

نتيجة فحص الكود سطرًا بسطر، لا تخمينًا:

| المكوّن | الحالة | الدليل |
|---|---|---|
| واجهة المستخدم | **حقيقية** ومكتملة بصريًا | 4 صفحات تعمل |
| CRUD المشاريع والرسائل | **حقيقي** ويُحفظ في Postgres | `routes/projects.ts` |
| حدود الخطة | **تُحسب فعليًا**، والترقية المجانية مغلقة | `plan-limits.ts` · `subscription.ts` |
| رفع الفيديو | ✅ **حقيقي** — إلى Supabase Storage، تقدّم من البايتات | `lib/video-storage.ts` |
| الذكاء الاصطناعي | **وهمي** — 11 جملة جاهزة بمطابقة كلمات | `messages.ts:17` |
| المونتاج والتصدير | **وهمي** — تقليب حالة بعد 5 ثوانٍ | `exports.ts:90` |
| المصادقة | ✅ **حقيقية** — Supabase Auth، توقيع ES256 عبر JWKS | `middlewares/auth.ts` |
| القوالب (MrBeast/Hormozi) | **غير موجودة** | صفر تطابق في الكود |

### المشاكل الثلاث الحرجة — أُغلقت جميعًا

**1. تسريب بيانات مفتوح.** ✅ أُغلق في المرحلة ١. كل مسار بيانات يمرّ بـ`requireAuth`، وكل استعلام مقيَّد بـ`user_id`. الوصول لبيانات غيرك يعيد **404 لا 403**، حتى لا يُستخدم رمز الخطأ لاستكشاف ما هو موجود.

**2. تلف بيانات بالتصميم.** ✅ أُغلق في المرحلة ٢. البايتات تذهب إلى حاوية `videos` الخاصة، وقاعدة البيانات تخزّن **مفتاح الكائن** لا رابطًا موقّتًا، والتشغيل يولّد رابطًا موقّعًا قصير العمر عند الحاجة. صفوف `blob:` القديمة مُسحت.

**3. الترقية مجانية.** ✅ أُغلق في المرحلة ١. أي ترقية بلا وسيلة دفع تُرفض بـ402.

**ما يحرسها:** `tools/isolation-test.mjs` — ٣٢ فحصًا على كود المصادقة الحقيقي (توقيع مزيّف، رمز منتهٍ، قراءة/كتابة/حذف عبر المستخدمين، ملكية مفاتيح التخزين). و`tools/storage-isolation.browser.js` لسياسات Storage على المشروع الحيّ.

---

## المعمار المستهدف

القرار المحوري: **معالجة الفيديو لا يمكن أن تعيش على Vercel** (لا ffmpeg، حد حزمة 250MB، مهلة تنتهي قبل انتهاء أي معالجة جادة). الحل طويل الأمد:

```
المتصفح ──رفع موقّع──► Supabase Storage (الوسائط)
   │                          ▲
   ▼                          │ الناتج المعالج
Vercel (واجهة + API) ──► Postgres: جدول jobs (الطابور)
                               │
                               ▼
                    عامل معالجة مخصص (ffmpeg)
                    Fly.io أو Railway — حاوية دائمة
```

**لماذا هذا التصميم:**

- **الفيديو المعالج يُعرض من منصتنا** — وهو ما طلبته. الناتج يُكتب في Storage الخاص بنا ويُقدَّم برابط دائم، لا تنزيل خام فقط.
- **بلا رسوم لكل دقيقة.** الخدمات الجاهزة (Shotstack/Cloudinary) تأخذ ~$0.05–$0.20 لكل دقيقة فيديو. عاملنا الخاص يكلّف ~$5–$20 شهريًا ثابتة مهما زاد الحجم.
- **بلا سقف على الميزات.** أي شيء يفعله ffmpeg متاح لنا: قص الصمت، زووم، ترجمة محروقة، مؤثرات، B-roll. الخدمات الجاهزة تحصرك في قدراتها.
- **الطابور في Postgres** الموجود أصلًا — لا نضيف Redis ولا مزوّدًا رابعًا قبل الحاجة.

**التكلفة الشهرية المتوقعة عند الإطلاق:** Vercel مجاني · Supabase مجاني (1GB تخزين) · العامل $5 · مفتاح OpenAI حسب الاستخدام (~$0.006 للدقيقة تفريغًا صوتيًا). أي: **أقل من $10 شهريًا حتى أول عشرات المستخدمين.**

---

## المراحل

### المرحلة ١ — الأساس الآمن ✅ مكتملة ومنشورة

بلا هذه المرحلة كل ما يُبنى فوقها يجب أن يُعاد بناؤه.

1. **Supabase Auth** — تسجيل ودخول بالبريد، ولاحقًا Google. نستخدم Supabase الموجود، بلا مزوّد جديد.
2. **ملكية البيانات** — عمود `user_id` على `projects` و`messages` و`exports`، واشتراك لكل مستخدم بدل الصف الوحيد `default`.
3. **Middleware للمصادقة** — يتحقق من JWT ويحقن `req.userId`؛ كل استعلام يُقيَّد بالمالك.
4. **إغلاق CORS** على النطاقات المعروفة بدل `*`.
5. **تجميد الترقية المجانية** حتى تُربط بوابة الدفع.

**المخرج:** لا يرى أحد بيانات أحد. الأساس صالح للبناء عليه.

### المرحلة ٢ — الرفع والتخزين الحقيقيان ✅ مكتملة ومنشورة

1. ✅ حاوية `videos` خاصة، وسياسات RLS تحصر كل كائن في `<user_id>/<project_id>/`.
2. ✅ رفع مباشر من المتصفح إلى Storage عبر `XMLHttpRequest` — التقدّم من `upload.onprogress`، أي من البايتات على السلك فعلًا.
3. ✅ صفوف `blob:` مُسحت، والخادم يرفض أي مفتاح تخزين خارج مجلّد صاحبه.
4. ✅ التشغيل عبر رابط موقّع — الفيديو يبقى بعد إعادة التحميل ومن أي جهاز.
5. ✅ حذف المشروع يستعيد بايتاته من التخزين.

**تنازل مفروض من المنصّة:** السقف الحقيقي للملف الواحد على خطة Supabase المجانية هو **50 ميغابايت**، لا 1 غيغابايت كما كانت الواجهة تدّعي. رفعه يتطلّب Supabase Pro (~$25 شهريًا). نصّ الواجهة صار يقول الحقيقة.

**المخرج المتحقق:** الفيديو يُرفع ويبقى ويُشاهَد — تحققتُ منه على الموقع الحيّ برفع ملف فعلي، ثم إعادة تحميل الصفحة كاملة والفيديو ما زال يعمل.

### المرحلة ٣ — خط المعالجة ⟵ نبدأ هنا

1. جدول `jobs` كطابور، مع حالات ومحاولات إعادة.
2. عامل معالجة على Fly.io يسحب المهام وينفّذها بـffmpeg.
3. أول عمليتين حقيقيتين: **قص الصمت** و**الترجمة المحروقة**.
4. الناتج يُكتب في Storage ويظهر داخل المنصة.

**المخرج:** المنتج يفعل ما يعد به. هنا يصبح Editly حقيقيًا.

### المرحلة ٤ — الذكاء الفعلي

1. طبقة مزوّد قابلة للاستبدال — تعمل بلا مفتاح الآن، ويُضاف مفتاح OpenAI بسطر واحد لاحقًا.
2. تفريغ صوتي بـWhisper → نص بطوابع زمنية (أساس الترجمة وقص الصمت الذكي).
3. تحويل الأمر الطبيعي ("اجعله مثل Hormozi") إلى **خطة مونتاج JSON** ينفّذها العامل.

**المخرج:** الوعد الأساسي "صف النتيجة، لا تحرّرها" يعمل فعلًا.

### المرحلة ٥ — المنتج والنمو

1. **القوالب** (MrBeast، Hormozi، Podcast…) — وهي في جوهرها خطط مونتاج محفوظة، تصبح رخيصة بعد المرحلة ٤.
2. **علامة "Edited with Editly"** على فيديوهات الخطة المجانية — حلقة النمو في رؤيتك؛ كل فيديو يصبح إعلانًا.
3. **الدفع** عبر Stripe، وربطه بالحدود المطبّقة أصلًا.
4. الإحالة والعمولة.

---

## ملاحظات على الرؤية الأصلية

قرأت وثيقة الفكرة. ثلاث ملاحظات صادقة:

**"وفّر 3 ساعات" أقوى من أي ميزة.** رؤيتك تقول هذا صراحةً وهي محقّة — لكن النسخة الحالية من الصفحة الرئيسية تبيع ميزات تقنية ("Sub-second AI response times")، لا وقتًا. تعديل النصوص رخيص وأثره كبير، وسأقترحه مع المرحلة ٣.

**العلامة المائية ليست ميزة مؤجَّلة، بل قناة التوزيع.** أوصي بتقديمها إلى المرحلة ٣ فور وجود معالجة حقيقية — إضافتها في ffmpeg سطر واحد، وقيمتها التسويقية تفوق أي ميزة في المرحلة ٥.

**القوالب قبل الذكاء العام.** "Make it like MrBeast" كقالب محفوظ أسهل بكثير وأدق نتيجةً من محاولة فهم أي أمر مفتوح. أنصح بإطلاق 3 قوالب متقنة قبل الوعد بفهم كل شيء — النتيجة أفضل والمخاطرة أقل.

---

## ما يحتاج قرارًا منك لاحقًا

| متى | القرار |
|---|---|
| **الآن — يمنع التسجيل فعليًا** | **البريد.** Supabase يطلب تأكيد البريد عند التسجيل، لكن مُرسِله المدمج لا يصل إلا لأعضاء مؤسّستك ومحدود بـ~رسالتين في الساعة. عمليًا: **أي مستخدم جديد لا يستطيع تفعيل حسابه.** الخياران: (أ) ربط مزوّد بريد حقيقي (Resend مجاني حتى 3000 رسالة شهريًا) وإبقاء التأكيد، أو (ب) إيقاف تأكيد البريد مؤقّتًا فيعمل التسجيل فورًا ونعيده لاحقًا. لم أغيّر الإعداد لأنه قرار أمني يخصّك. |
| قريبًا | `SUPABASE_SERVICE_ROLE_KEY` في Vercel — يجعل استعادة مساحة التخزين مضمونة من الخادم بدل الاعتماد على المتصفح، ويحتاجه عامل المعالجة في المرحلة ٣ على أي حال. |
| المرحلة ٣ | حساب Fly.io أو Railway للعامل (~$5 شهريًا) |
| المرحلة ٣ | خطة Supabase Pro إن أردنا تجاوز سقف 50 ميغابايت للملف |
| المرحلة ٤ | مفتاح OpenAI |
| المرحلة ٥ | حساب Stripe، ونطاق خاص بدل `editly-eta.vercel.app` |
